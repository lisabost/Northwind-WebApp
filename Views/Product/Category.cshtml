@using Northwind.Models
@model IEnumerable<Category>

<div class="list-group">
    @foreach (Category c in Model)
    {
        <a href="#" class="list-group-item list-group-item-action">@c.CategoryName - @c.Description</a>
    }

</div>
